import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lavabot.app',
  appName: 'Lava-Bot',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
